{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Spec Document(Issue) Title"
    },
    "body": {
      "type": "string",
      "description": "Spec Document(Issue) Body"
    }
  },
  "required": [
    "title",
    "body"
  ],
  "additionalProperties": false
}